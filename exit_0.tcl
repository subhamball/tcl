puts "Before exit"
exit 0
puts "After exit"
#The exit 0 command is a common usage in Tcl scripts and it means that the script is exiting successfully with a status code of 0.
#In Unix-like systems (e.g., Linux, macOS), a status code of 0 indicates successful execution, while a non-zero status code typically indicates an error or some other kind of abnormal termination.